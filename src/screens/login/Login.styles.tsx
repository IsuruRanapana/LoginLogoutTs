import {StyleSheet} from 'react-native';

let styles;
export default styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
    },
    margin: {
        height: 25,
    },
    marginBottom: {
        height: 120,
    },
    inputContainer: {
        alignItems: 'center',
        width: '100%',
    },
    btn: {
        width: '100%',
        alignItems: 'center',
    },
});
